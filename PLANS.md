# Execution Plans

plan_schema_version: 2

Use this file for active, blocked, ready-for-closure, or recently completed execution work. The canonical lifecycle is the installed `engineering-workflow` planning reference.

## Active Plan: Xeonvs Engineering 1.0.4 Brand And Plugin Icons

Status: active
Owner: root
Last Updated: 2026-09-12

### Goal

Publish marketplace release `v1.0.4` with the approved Xeonvs Engineering
identity and exact icon-bearing `engineering-workflow` 0.9.4 and `tgrep-search`
1.0.2 bundles, then verify the installed Codex and Claude copies.

### Plan Origin

direct_execution

### Requested Scope

- Add the approved opaque marketplace mark as repository branding without
  inventing an unsupported marketplace icon manifest field.
- Import both newly released icon-bearing plugin packages through the existing
  immutable-tag synchronization path and verify their provenance and bytes.
- Release and refresh the unified marketplace so Codex displays the plugin
  logos; preserve Claude compatibility while its current schema lacks image
  presentation fields.

### Requirement Traceability

| Requirement | Complete outcome | Source | Work queue | Acceptance or validation | Status |
| --- | --- | --- | --- | --- | --- |
| REQ-001 | Approved marketplace X/core mark exists as canonical SVG and opaque PNG branding and appears in README. | User approval, 2026-09-12 | WQ-01 | XML, dimensions/opacity, README render/link, and visual QA pass. | done |
| REQ-002 | Catalog bundles exactly match released engineering-workflow 0.9.4 and tgrep-search 1.0.2, including valid Codex icon metadata and assets. | User delivery request | WQ-02 | Sync-generated provenance/digests, package identity, and recorded-upstream verification pass. | pending |
| REQ-003 | Marketplace release v1.0.4 is immutable, clean, and contains only intended branding and exact plugin packages. | User version-bump decision | WQ-03 and Post-Close Delivery | Tests, catalog validation, security, signed tag, release CI, archive checksum/file audit. | pending |
| REQ-004 | Codex and Claude marketplace/install readbacks resolve the new catalog and plugin versions; Codex assets render through supported fields. | User local-update request | Post-Close Delivery | Native update/install commands, manifests, active cache paths, byte parity, and UI inspection. | pending |

### Explicit Non-Goals

- Add a marketplace-level image field unsupported by current schemas.
- Modify plugin runtime instructions or hand-edit imported bundle bytes.
- Claim Claude renders icons when its documented manifest schema does not
  expose an image field.

### Constraints

- Plugin changes arrive only through released source tags and the existing sync
  automation; local catalog edits must not falsify provenance.
- The root brand image is fully opaque and contains no generated preview noise.
- Preserve read-only default Actions permissions and review-required sync PRs.

### Inputs And Sources

- User-approved third-round marketplace mark: four cobalt modules around one
  teal catalog core.
- Planned source releases: engineering-workflow 0.9.4 and tgrep-search 1.0.2.
- Claude Code plugin schema:
  `https://code.claude.com/docs/en/plugins-reference`.

### User Decisions And Answers

- 2026-09-12: Approve all three no-hole concepts.
- 2026-09-12: Raise package versions to enable immediate immutable marketplace
  delivery.

### Completed Baseline State

- [x] WQ-00 — Clean synchronized `main` at
  `1cae1308c4dba16505e230d2b76b9ae9f8153739`; latest marketplace release is
  v1.0.3 and current bundles are engineering-workflow 0.9.3 and tgrep-search 1.0.1.
- [x] WQ-00 — Approved marketplace SVG and opaque 1024/128 px previews exist in
  task-owned staging before repository implementation.

### Current Work Queue

- [x] WQ-01 — Add and validate marketplace branding for REQ-001. `done`
- [ ] WQ-02 — After both source releases, run sync, review the generated PR, and validate REQ-002. `pending`
- [ ] WQ-03 — Complete aggregate/security/release preparation for REQ-003 and REQ-004. `pending`

### Locked Decisions

- Use navy `#101828`, cobalt `#3972F6`, and teal `#2DD4BF`; the shipped image
  is entirely opaque.
- Keep marketplace branding in `assets/` and README because current Codex and
  Claude marketplace schemas do not define a top-level logo field.
- Publish v1.0.4 only after exact source-tag and sync-PR evidence is green.

### Verification

- REQ-001 / WQ-01: SVG parse, deterministic PNG render, dimensions/opacity,
  README path check, and visual inspection.
- REQ-002 / WQ-02: `python3 -m unittest discover -s tests -v`,
  `python3 scripts/validate_catalog.py`, and
  `python3 scripts/sync_upstreams.py --verify-recorded` on the generated bytes.
- REQ-003 / WQ-03: full diff review, `git diff --check`, redacted Gitleaks tree
  and reachable-history scans, hosted CI, signed tag, release archive SHA/file
  parity, and fresh public clone.
- REQ-004 / Post-Close Delivery: native Codex/Claude marketplace and plugin
  update commands, active manifest/cache byte readback, and Codex UI inspection.

### Latest Validation Results

- 2026-09-12: Marketplace SVG parsed; the PNG is 1024x1024 opaque RGB and the
  approved mark passed full/compact visual review. README embeds the SVG at a
  bounded 160 px width with meaningful alt text.
- 2026-09-12: All 15 current tests, catalog validation, recorded-upstream
  verification for the unchanged baseline bundles, active-plan validation, and
  whitespace checks passed. Catalog manifests contain no unsupported logo field.
- 2026-09-12: Initial all-plugin sync run `34695567427` correctly failed closed
  before creating a PR because public hygiene rejected the new plugin PNGs as
  non-text artifacts. The validator now derives a narrow allowlist only from
  complete, confined Codex `composerIcon`/`logo`/`logoDark` paths, validates
  exact dimensions and opaque RGB encoding, rejects image fields in Claude
  manifests, and still rejects any undeclared binary. All 16 tests plus current
  baseline catalog/provenance validation pass after the correction.

### Risks And Recovery

- Risk: sync selects only one new package. Recovery: dispatch the configured
  all-plugin sync after both releases and reject a PR missing either exact tag.
- Risk: root branding is mistaken for a supported marketplace avatar. Recovery:
  keep it out of manifest schemas and describe its README/repository role.
- Risk: installed caches retain prior same-name packages. Recovery: refresh the
  correct marketplace source, reinstall/update natively, and inspect resolved
  version/path/bytes rather than editing caches.

### Resume Point

- Continue WQ-02 by merging the validated binary-asset guard, then repeat the
  all-plugin synchronization workflow and review its exact generated PR.

### Plan Fidelity Check

- [x] Every agreed outcome has a requirement ID.
- [x] Every source URL is preserved.
- [x] Every user answer and locked decision is preserved.
- [x] No requirement was silently narrowed or removed.
- [x] The queue covers every requirement ID.
- [x] Validation covers every acceptance criterion.
- [x] Non-goals do not contradict requested scope.
- [x] The resume point names the first unfinished queue item.
- [x] This plan is not a compressed rewrite of a more detailed approved plan.

### Reconciliation Check

- [ ] Changed results and affected plan entries agree.
- [ ] Plan, provenance, validation, working tree, and historical sections agree.
- [ ] Completed sections contain no stale unfinished-work wording.

### Closure Gate

- [ ] Every in-scope requirement and queue item is terminal.
- [ ] Applicable validation is current for final content.
- [ ] Review feedback, omissions, and indexes are reconciled.
- [ ] Resume Point contains no future in-scope work.
- [ ] Compact closure can be applied atomically.

### Post-Close Delivery

- Authorized commits, protected PR merges, signed source and marketplace tags,
  GitHub releases, hosted CI/readback, and local Codex/Claude refresh are
  outside the closed implementation scope and proceed only after all gates pass.

### Handoff Notes

- None.

## Recently Completed

- [x] 2026-09-12: Completed Refresh Marketplace Actions For Node 24.
- [x] 2026-09-12: Completed Repair Marketplace Sync Authentication For Engineering Workflow 0.9.2.

## Completed Work

### xeonvs-engineering 1.0.1 — initial unified marketplace

- **Status:** completed
- **Release classification:** no-release closure (the artifact-bearing marketplace release is `v1.0.1`)
- **Goal:** publish one portable marketplace named `xeonvs-engineering` with
  current versioned public packages `engineering-workflow` and `tgrep-search`;
  later package updates create a Dependabot-style, review-required Draft PR.
- **Scope:** identical local-bundle catalogs for Codex and Claude Code; exact
  provenance; deterministic identity, byte, package, and public-content
  validation; sync automation; Draft review; immutable GitHub release; native
  direct-skill guidance for OpenCode and other agents.
- **1.0.1 release source inputs:** `engineering-workflow` **0.9.1** from
  `xeonvs/codex-engineering-workflow` immutable annotated tag `v0.9.1` at
  `80c6a39eab44f9a78f492adb91811b447da6c73d`. `tgrep-search` **1.0.1** comes
  from immutable release tag `v1.0.1` at
  `e316614144d14efb7bdf63f49ff820a12bdedc84`.
- **Non-goals:** alter either upstream repository; import arbitrary unversioned
  changes; include binaries, installers, hooks, MCP servers, telemetry,
  credentials, local indexes, or a separate OpenCode marketplace.
- **Security boundary:** automation treats upstream contents as untrusted data,
  accepts only configured public repositories and source policies, imports only
  a named bundle after manifest/version validation, and opens one Draft PR. It
  never merges, tags, publishes, or changes an installed skill.

| ID | State | Work and acceptance evidence |
| --- | --- | --- |
| XM-01 | done | Initial public catalog repository exists. |
| XM-02 | done | Codex/Claude catalogs, verified bundles, provenance, hygiene checks, and source-byte parity exist; stale `engineering-workflow` 0.8.2 import was corrected to 0.9.1. |
| XM-03 | done | PR #2 was reviewed and hosted CI passed. PR #3 added portable cache exclusions and a release-time archive assertion; signed immutable catalog release [`v1.0.1`](https://github.com/xeonvs/xeonvs-engineering/releases/tag/v1.0.1) was published with a clean archive. |
| XM-04 | done | Codex registered the catalog from `v1.0.1`, installed and discovered `tgrep-search@xeonvs-engineering` version `1.0.1`, and read it back enabled. The superseded direct local skill was then removed after the marketplace-managed installation was read back; no other agent installation changed. |
| XM-05 | done | The bounded weekday/manual sync workflow is merged and validated. It detects eligible newer versioned packages and can create or refresh exactly one review-required Draft PR; it never merges, tags, publishes, or changes an installed skill. |

## Completion

Completed on 2026-09-09. Marketplace release [`v1.0.1`](https://github.com/xeonvs/xeonvs-engineering/releases/tag/v1.0.1) contains only intended source assets and checksummed bundles; at that release boundary both catalogs resolved the verified packages recorded above. The sync workflow remains review-only and can produce one Draft PR for a newer eligible source package. Codex installed and discovered enabled `tgrep-search@xeonvs-engineering` `1.0.1`; the superseded direct local skill was removed after managed-install readback, and no unrelated agent installation changed.

This is a documentation and closure record only. It changes no shipped plugin bundle, provenance, or marketplace artifact contract, so it does not create a new marketplace release.
