# Agent Execution Incidents

incident_schema_version: 1

This is a non-normative catalog of recurring failure classes. Canonical rules live under the Owner links; routes make them reachable and guards enforce them. Entry fields and lifecycle are defined by the installed `engineering-workflow` reference `references/instruction_lifecycle.md`.

## Entries

No recurring incident has been recorded yet.
