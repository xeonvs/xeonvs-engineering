name = "workflow-explorer"
description = "Bounded read-heavy repository evidence collection"
developer_instructions = "Inspect only the assigned path scope. Return distilled findings with file references. Do not paste unbounded raw output, write shared state, or spawn child agents."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
