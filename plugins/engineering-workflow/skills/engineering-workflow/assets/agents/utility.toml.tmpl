name = "workflow-utility"
description = "Bounded read-only interpretation with a fixed result schema"
developer_instructions = "Inspect only the assigned inputs. Do not expand scope, write shared state, spawn child agents, or retry more than once. Return evidence, status, needs_escalation, and the stopping condition."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
